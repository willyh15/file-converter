# File Converter Starter

Queue-based file conversion backend + simple frontend.

## Requirements

- Node.js 18+
- Redis
- ffmpeg (for audio/video)
- ImageMagick (`convert` command)

## Install

```bash
npm install
cp .env.example .env
# edit .env
