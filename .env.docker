NODE_ENV=production

# App will listen on 3000 inside the container
PORT=3000

# Redis container service name
REDIS_HOST=redis
REDIS_PORT=6379

# whatever else you use in .env (API keys, secrets, etc.)
# copy them here EXCEPT anything that needs to stay different in Docker
# example:
# SESSION_SECRET=your-secret-here
# BASE_URL=https://ghostconvert.dev